// ignore_for_file: prefer_const_constructors
import 'package:test/test.dart';
import 'package:umbra_cli/umbra_cli.dart';

void main() {
  group('UmbraCli', () {
    test('can be instantiated', () {
      expect(UmbraCli(), isNotNull);
    });
  });
}
