samplerUniforms: [{{#samplers}}
        ImageShader(
          {{name.camelCase()}}{{extension}},
          TileMode.clamp,
          TileMode.clamp,
          UmbraShader.identity,
        ),{{/samplers}}
      ],